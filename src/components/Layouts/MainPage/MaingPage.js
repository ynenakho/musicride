import React from 'react';

import styles from './MainPage.css';

const layout = (props) => {
    return (
        <div className="styles.MaingPage">
            
        </div>
    );
};

export default layout;