import React from 'react';
import { Form, Button, Col, Container, Row } from 'react-bootstrap'

const home = (props) => {
	return (
		<div>
			<Container>
				
			</Container>
		</div>
	);
}

export default home;